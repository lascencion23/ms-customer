spring:
  application:
    name: ms-customer
  cloud:
    config:
      uri: ${CONFIG_URI}
      fail-fast: true
      retry:
        max-attempts: 20
        max-interval: 15000
        initial-interval: 10000
      profile: ${ENVIRONMENT}
  profiles:
    active: ${ENVIRONMENT}